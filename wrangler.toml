# Static site only — no Functions, no runtime dependencies. Generation runs in Claude Code
# on the subscription, not on Pages; see "Model access" in README.md.
#
# `name` must match the Pages project exactly or the build fails.

name = "saulera-dossier-engine"
pages_build_output_dir = "./public"

compatibility_date = "2026-07-27"
