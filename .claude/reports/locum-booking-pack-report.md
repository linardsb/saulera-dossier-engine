# Implementation Report — Locum booking pack (#49)

**Plan**: `.claude/plans/locum-booking-pack.md`   **Branch**: `feat/locum-booking-pack` (cut from `origin/main` after PR #52 merged)   **Status**: COMPLETE

## Summary

The submission pack gains a locum variant through the one canonical contract: three new claim sections — `compliance`, `booking`, `modality_matrix` — enter `PACK_SCHEMA`, are enforced by `assertPack`/`verifyPack`/`provenanceSummary` under the existing demote-don't-drop provenance rule, are generated by an extended locum branch of `domainBlock`, and render first in both existing renderers under the title `LOCUM BOOKING — {role_title}` when the pack's own `role_shape` is locum. Perm packs are byte-identical to before — zero existing snapshots changed.

## Tasks completed

- Three locum sections + `LOCUM_SECTIONS` + tolerant-on-absence `assertPack` (per-claim check factored out) → `src/pack.js` (UPDATE)
- `verifyPack`/`provenanceSummary` iterate both section lists, guarded; legacy packs gain nothing → `src/provenance.js` (UPDATE)
- Booking-pack bullets in the locum branch of `domainBlock` (fill the sections; verbatim-quote rule; labels carry the brief's ask, claims carry candidate evidence; note quirks surfaced only when recorded) → `src/prompt.js` (UPDATE)
- Booking sections render first, title branch on `pack.role_shape ?? "unknown"`, existing citation machinery → `src/render/appendix.js`, `src/render/inline.js` (UPDATE)
- Locum fixture (Priya-Nair-flavoured MRI/CT radiographer) → `test/fixtures/pack-locum.json` (CREATE) + 4 snapshots (CREATE, all read against the "say yes today" bar)
- Forward-reference + amendment recording the `locumFieldFor` divergence → `.claude/plans/client-note-locum-fields.md` (UPDATE)

## Tests added

- `test/paste.test.js` — locum round trip intact; malformed item rejected naming the section; legacy pack passes without gaining keys (3 tests)
- `test/provenance.test.js` — fabricated compliance quote demoted with `failed_quote` and `section: "compliance"`; summary counts all three sections; legacy pack unchanged (3 tests)
- `test/prompt.test.js` — locum instructions on both prompt shapes; absent for permanent imaging (2 tests)
- `test/render.test.js` — locum in the snapshot loop (4 snapshots); title branch; sections lead before AGAINST THE BRIEF; unverified compliance marked with no citation/source line; perm fixtures free of locum headings; empty-sections/disobedient-model edge cases; page budget (7 tests + loop extension)
- `test/generate.test.js` — the locum brief's request block names the three sections (1 assertion)

All results: pass.

## Validation results

- `node --check` on all five changed src files: pass
- `npm test` under Node 24.11.0: **778 pass, 0 fail, 0 skipped**
- (Under this machine's default Node 20 the suite's own version-guard test fails by design and the D1 integrity tests skip — pre-existing behaviour, unrelated to this change)
- `git status test/fixtures/`: only ADDED files (`pack-locum.json` + 4 locum snapshots); zero modified existing snapshots
- Manual render of the locum fixture: leads with LOCUM BOOKING title, compliance, availability/rate, scanner matrix — a client could confirm from it

## Deviations from the plan

- **One existing test line adjusted** (`test/prompt.test.js`, "the domain block is empty for the nursing inputs"): its whole-prompt `doesNotMatch(/HCPC/)` now fails because the schema descriptions inside `OUTPUT_INSTRUCTION` legitimately name HCPC (schema growth the plan explicitly accepts, Out-of-Scope §"byte-identical prompt blocks"). The assertion is scoped to the prompt before `OUTPUT_INSTRUCTION`; the domain-block-is-empty assertion is untouched. This is the test catching exactly what it should and being re-aimed at its actual intent.
- **`locumFieldFor` not imported** (anticipated by the plan's A5): the note's locum vocabulary is consumed as static prompt wording. Recorded in the #48 plan's forward-references and amendments.
- Fixture label lengths were tuned (e.g. `MRI — GE Signa`) so the 78-column wrap doesn't split the render-test assertion spans — cosmetic only.

## Issues encountered

None beyond the above. The generate path needed zero code changes, as the plan predicted — `PACK_SCHEMA` rides `output_config.format` directly, confirmed by the existing identity assertion in `test/generate.test.js`.
